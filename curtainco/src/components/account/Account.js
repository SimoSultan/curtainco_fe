import React from 'react'

import {
    Container,
    Typography
} from '@material-ui/core'

function Account() {
    return (
        <Container>
            <Typography variant="h3">
                Account Page
            </Typography>
        </Container>
    )
}

export default Account
