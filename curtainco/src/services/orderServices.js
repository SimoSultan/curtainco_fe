const { __esModule } = require("react-router-dom/cjs/react-router-dom.min")

function createOrderFromCart(cart) {
    let order = {}
    return order
}

module.exports = {
    createOrderFromCart,
}
